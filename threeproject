<views.py>

from django.shortcuts import render

def index(request):
    return(request, "index.html", render)


<urls.py>

from django.contrib import admin
from django.urls import path
from home import views

urlpatterns = [
    path('', views.index),
]
